# Add project specific ProGuard rules here.
# This project ships with minifyEnabled false by default (see app/build.gradle.kts),
# so this file only matters once you turn minification on for a release build.
